# Probes

Small platform experiments live here before they become engine backends or tests.

## macOS Bookmark Probe

`macos_bookmark_probe` explores security-scoped `NSURL` bookmark behavior for user-selected folders. It stores its generated token under `local/probes/`, which is intentionally gitignored.
